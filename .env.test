# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# No real mail in tests; messages are still recorded for assertions.
MAILER_DSN=null://null
# Keep uploaded test files out of the real share dir; under var/ (gitignored).
APP_SHARE_DIR=var/test-uploads
