twig:
    file_name_pattern: '*.twig'
    globals:
        # Centre name shown in e-mails/notices; generic by default, real value set per environment.
        app_organization_name: '%env(APP_ORGANIZATION_NAME)%'

when@test:
    twig:
        strict_variables: true
